import React from "react";
import TopMenuButton from "./topMenuButton/TopMenuButton";

const ContactButton = () => {
  return <TopMenuButton index="04" title="<Contact/>" />;
};

export default ContactButton;
