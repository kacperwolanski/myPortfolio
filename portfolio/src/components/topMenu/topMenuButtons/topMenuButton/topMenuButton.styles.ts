import styled from "styled-components";
import theme from "../../../../theme/theme";

export const TMButtonContainer = styled.div`
  color: white;
`;

export const IndexContainer = styled.span`
  color: ${theme.palette.primary.main};
`;
